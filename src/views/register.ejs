<!DOCTYPE html>
<html lang="en">

<head>
  <title>Register - InquizitorApp</title>
  <%- include("partials/_imports.ejs") %>
</head>

<body>

  <%- include("partials/_header.ejs", {alerts, userData, currentPage, data: {}}) %>

  <main class="container d-flex flex-column flex-lg-row mt-lg-4">

    <!-- MAIN WINDOW (FULL) -->
    <div class="col-12">
      <section id="main-full" class="border-yellow-darker form-custom d-flex flex-column flex-lg-row align-content-between justify-content-lg-around">

        <!-- FORM -->
        <div class="col-lg-5 d-flex flex-column justify-content-center">
          <h1 class="h3">Sign up</h1>
          <form class="form" action="/register" method="POST">
            <div class="form-group mb-2">
              <!-- USERNAME -->
              <label for="username" class="form-label text-muted">Username</label>
              <input class="form-control" type="text" name="username" maxlength="12" required>
              <!-- EMAIL -->
              <label for="email" class="form-label text-muted mt-2">Email</label>
              <input class="form-control" type="email" name="email" maxlength="60" required>
              <!-- PASSWORD -->
              <label for="password" class="form-label text-muted mt-2">Password</label>
              <input class="form-control" type="password" name="password" maxlength="80" required>
              <!-- SUBMIT -->
              <button type="submit" class="btn-custom btn-custom-flex mt-5">Sign up</button>
            </div>
          </form>
          <p class="text-muted mt-4">Already have an account? <a href="/login">Log in</a>.</p>
        </div>
        <span class="divider-v d-none d-md-inline border-yellow-darker"></span>
        <span class="divider-h d-inline d-lg-none border-yellow-darker"></span>
        <div class="col-lg-5 d-flex flex-column justify-content-start">
          <h1 class="h3 mt-3">You'll be able to:</h1>
          <div class="mt-4 lead">
            <p>✏️ Publish your own quizzes</p>
            <p>⭐ Submit quiz reviews</p>
            <p>❤️ Save your favorite quizzes</p>
            <p>⚙️ Customize your profile</p>
            <p>📚 Access free study tools</p>
          </div>
          <h1 class="h4 mt-4">...and it's totally <span class="highlight">FREE</span>!</h1>
        </div>

      </section>
    </div>

  </main>

  <%- include("partials/_footer.ejs") %>

</body>

</html>